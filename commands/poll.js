const { Message } = require("discord.js");
const { user, channel } = require('../config.json');

module.exports = {
    name : 'poll',
    description : 'command for a user to run a poll in a channel using reactions to the message',
    execute(message) {

        
    }
}